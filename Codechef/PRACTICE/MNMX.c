#include <stdio.h>

int main(int argc, char const *argv[])
{
	int T, N;
	scanf("%d", &T);

	while (T--)
	{
		scanf("%d", &N);
		int A[N];
		int i, j;
		for(i = 0;i < N;i++)
		{
			scanf("%d", &j);
			A[i] = j;
		}
		
	}
	return 0;
}